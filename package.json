{
  "name": "codev",
  "version": "1.0.0",
  "description": "Real-time collaborative code editor",
  "main": "index.js",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && npm start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../backend && npm install",
    "render-build": "npm run install-all && npm run build",
    "render-start": "npm start"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Sahil1607cms/Codev.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Sahil1607cms/Codev/issues"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.8.1",
    "dotenv": "^17.2.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
