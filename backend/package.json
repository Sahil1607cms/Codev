{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for Codev - Real-time collaborative code editor",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
  "dev": "nodemon src/server.js",
  "start": "NODE_ENV=production node --max-old-space-size=512 src/server.js",
  "build": "npm install && cd ../frontend && npm install && npm run build"
},
  "engines": {
    "node": "18.x"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^4.18.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
